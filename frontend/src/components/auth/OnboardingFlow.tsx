"use client";
import Link from "next/link"; import { useState } from "react"; import { ArrowRight, Check } from "lucide-react"; import { Button } from "@/components/ui/button";
const steps = [["Plant context", "North Fabrication Plant / Line A is a simulated demonstration context."], ["Team roles", "Production, maintenance, warehouse, procurement, and logistics retain scoped decisions."], ["Explore WS-102", "Start at the Haas CNC to follow the bearing-risk recovery path."]] as const;
export function OnboardingFlow() { const [index, setIndex] = useState(0); const [complete, setComplete] = useState(false); return <main className="grid min-h-screen place-items-center bg-background p-5"><section className="w-full max-w-2xl rounded-2xl border border-border bg-card p-7"><p className="font-mono text-xs tracking-widest text-muted-foreground">DEMO ONBOARDING / STEP {index + 1} OF 3</p><h1 className="mt-4 font-heading text-4xl font-semibold tracking-[-0.04em]">{complete ? "Ready to explore the scenario." : steps[index][0]}</h1><p className="mt-4 max-w-xl text-base leading-7 text-muted-foreground">{complete ? "The WS-102 guided scenario is ready. Every visible value remains demo data." : steps[index][1]}</p><div className="mt-8 flex gap-2">{steps.map((step, itemIndex) => <span key={step[0]} className={`h-1.5 flex-1 rounded-full ${itemIndex <= index ? "bg-primary" : "bg-muted"}`} />)}</div><div className="mt-8 flex flex-wrap justify-between gap-3"><Button asChild variant="ghost"><Link href="/dashboard">Skip setup</Link></Button>{complete ? <Button asChild><Link href="/dashboard">Open WS-102 scenario <ArrowRight data-icon="inline-end" /></Link></Button> : <Button onClick={() => index === 2 ? setComplete(true) : setIndex(index + 1)}>{index === 2 ? <Check data-icon="inline-start" /> : null}{index === 2 ? "Finish" : "Continue"}</Button>}</div></section></main>; }
