"use client"; import { useEffect } from "react"; import { Button } from "@/components/ui/button";
export default function Error({ reset }: { error: Error & { digest?: string }; reset: () => void }) { useEffect(() => {}, []); return <main className="grid min-h-[60vh] place-items-center p-5"><section className="max-w-md text-center"><p className="font-mono text-xs text-destructive">DEGRADED DEMO VIEW</p><h1 className="mt-3 font-heading text-3xl font-semibold">This scenario view could not load.</h1><p className="mt-3 text-sm text-muted-foreground">No equipment action was taken. Retry the local demo view.</p><Button className="mt-5" onClick={reset}>Retry view</Button></section></main>; }
