import Link from "next/link"; import { ShieldX } from "lucide-react"; import { Button } from "@/components/ui/button";
export default function PermissionDeniedPage() { return <div className="grid min-h-[70vh] place-items-center p-5"><section className="max-w-lg text-center"><ShieldX className="mx-auto size-8" /><p className="mt-4 font-mono text-xs text-muted-foreground">ROLE-RESTRICTED DEMO VIEW</p><h1 className="mt-3 font-heading text-4xl font-semibold">This action is outside your current role.</h1><p className="mt-4 text-sm leading-6 text-muted-foreground">The destination remains visible for workflow context, but only an authorized role may approve the consequential action.</p><Button asChild className="mt-6"><Link href="/dashboard">Return to oversight</Link></Button></section></div>; }
