// Archived in response to the request to preserve superseded code rather than delete it.
// <div className="rounded-md border border-border bg-muted p-4">
//   <div className="flex items-center gap-2"><SlidersHorizontal className="size-4" /><p className="text-sm font-semibold">Assembly inspection guide</p></div>
//   <p className="mt-1 text-sm leading-6 text-muted-foreground">The retained Haas source is a single mesh. This control changes the inspection presentation, but cannot truthfully separate the bearing geometry until a segmented CNC asset is supplied.</p>
//   <input className="mt-4 w-full accent-black" type="range" min="0" max="100" value={inspectionProgress * 100} onChange={(event) => setInspectionProgress(Number(event.target.value) / 100)} aria-label="Assembly inspection progress" />
// </div>
